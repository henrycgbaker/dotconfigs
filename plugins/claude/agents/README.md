# Project Agents (stub)

Per-project agent persona files. Currently standalone reference material — not yet wired into the deploy pipeline.

Future: integrate with `dotconfigs project` to deploy agent configs to target repos.
