---
phase: 09-config-ux-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dotconfigs
  - dots
autonomous: true

must_haves:
  truths:
    - "`dotconfigs` is the primary executable (real script, not symlink)"
    - "`dots` is a convenience symlink pointing to `dotconfigs`"
    - "`dotconfigs list` says 'deployed' and 'not deployed' instead of 'installed' and 'not installed'"
    - "Banner text in entry point refers to 'dotconfigs' not 'dots'"
  artifacts:
    - path: "dotconfigs"
      provides: "Primary CLI executable"
      contains: "dotconfigs — Unified configuration management"
    - path: "dots"
      provides: "Convenience symlink to dotconfigs"
  key_links:
    - from: "dots"
      to: "dotconfigs"
      via: "symlink"
      pattern: "dots -> dotconfigs"
---

<objective>
Reverse the CLI naming established in quick-002 (currently `dots` is primary, `dotconfigs` is symlink — should be opposite) and fix list command terminology.

Purpose: Success criteria 9 (CLI naming) and 10 (list terminology) require `dotconfigs` as primary executable with `dots` as convenience alias, and correct "deployed"/"not deployed" wording.
Output: Renamed entry point files, fixed list output.
</objective>

<execution_context>
@/Users/henrybaker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/henrybaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@dots
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reverse CLI naming and fix terminology</name>
  <files>dotconfigs, dots</files>
  <action>
1. **Reverse naming:** Currently `dots` is the real script and `dotconfigs` is a symlink to `dots`. Reverse this:
   - Remove the `dotconfigs` symlink
   - Rename `dots` to `dotconfigs` (the real script file)
   - Create `dots` as a symlink pointing to `dotconfigs`
   - Use `git mv` or equivalent to preserve history

2. **Update file header:** In the now-primary `dotconfigs` file:
   - Change line 2 comment from `# dots —` to `# dotconfigs —`
   - Keep the rest of the script identical

3. **Update banner text:** In `show_usage()` and help text:
   - The usage block already references `dots` — this is fine for the shortcut name
   - But internal references to the tool name in banners should say `dotconfigs`
   - The `show_usage()` function header should read: `dotconfigs — Unified configuration management`
   - All `dots` references in the usage text are fine (they demonstrate the shortcut)
   - The `cmd_setup()` echo should say `dotconfigs — Tool Setup`

4. **Fix list terminology:** In `cmd_list()`:
   - Change `"$plugin installed"` to `"$plugin deployed"`
   - Change `"$plugin not installed"` to `"$plugin not deployed"`

5. **Fix PATH symlink:** In `_create_path_symlink()`:
   - Change target from `dots` to `dotconfigs` (the symlink on PATH should point to the primary executable)
   - Update the reference `$SCRIPT_DIR/dots` to `$SCRIPT_DIR/dotconfigs`

6. **Fix stale banner text in plugin wizards:** The context mentions stale "dotconfigs" references in plugin banners that should say "dots". BUT per the naming decision, `dotconfigs` IS the correct name. Check both `plugins/claude/setup.sh` and `plugins/git/setup.sh` for their banner text — these currently say "dotconfigs" which is now correct. No change needed there.

7. **Fix next-steps references:** Search for any `'dotconfigs deploy'` or `'dotconfigs setup'` references that should now show the shortcut form. Per decision: docs reference `dotconfigs`, so these are fine. But the quick reference in wizard next-steps can mention `dots` as shortcut. Leave as-is if they already say dotconfigs.
  </action>
  <verify>
- `ls -la dotconfigs` shows regular file (not symlink)
- `ls -la dots` shows symlink to dotconfigs
- `grep -c "not installed" dotconfigs` returns 0
- `grep -c "not deployed" dotconfigs` returns at least 1
- `head -2 dotconfigs` shows "# dotconfigs —"
- `readlink dots` returns "dotconfigs"
  </verify>
  <done>
- `dotconfigs` is the primary executable (real script file with shebang)
- `dots` is a convenience symlink pointing to `dotconfigs`
- `cmd_list()` outputs "deployed" / "not deployed"
- File header and banner text reference "dotconfigs" as tool name
- PATH symlink creation references the primary executable
  </done>
</task>

</tasks>

<verification>
- `./dotconfigs --help` runs successfully and shows usage
- `./dots --help` runs successfully (via symlink) and shows same output
- `./dotconfigs list` output contains "deployed" or "not deployed" (not "installed")
- `file dotconfigs` shows "shell script" (not symlink)
- `file dots` or `readlink dots` shows symlink
</verification>

<success_criteria>
1. `dotconfigs` is the primary executable, `dots` is a symlink
2. List command uses "deployed"/"not deployed" terminology
3. Banner text says "dotconfigs" as tool name
</success_criteria>

<output>
After completion, create `.planning/phases/09-config-ux-redesign/09-01-SUMMARY.md`
</output>
