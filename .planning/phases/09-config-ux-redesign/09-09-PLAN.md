---
phase: 09-config-ux-redesign
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/usage-guide.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "TODO marker removed from top of usage guide"
    - "All references to dotclaude changed to dotconfigs"
    - "Syncing section uses correct repo name and CLI commands"
  artifacts:
    - path: "docs/usage-guide.md"
      provides: "Up-to-date Claude Code configuration guide"
  key_links:
    - from: "README.md"
      to: "docs/usage-guide.md"
      via: "markdown link"
      pattern: "usage-guide"
---

<objective>
Fix `docs/usage-guide.md` — remove the TODO marker, update stale references (dotclaude → dotconfigs), and fix the syncing section.

Purpose: The usage guide has an explicit "TODO: is this up to date?" at the top and still references the old `dotclaude` repo name. These need fixing for documentation consistency.
Output: Clean usage guide with no TODO markers and correct project naming.
</objective>

<execution_context>
@/Users/henrybaker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/henrybaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/usage-guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove TODO and fix stale references in usage guide</name>
  <files>docs/usage-guide.md</files>
  <action>
    Read docs/usage-guide.md fully and apply these changes:

    **1. Remove the TODO marker (line 3):**
    Delete the line `*TODO: is this up to date?*` and the blank line after it.

    **2. Fix the "Syncing Config Across Machines" section (lines 660-672):**
    Replace `dotclaude` with `dotconfigs` in the two `cd` commands (lines 664, 669):
    ```bash
    cd ~/Repositories/dotconfigs
    ```
    Also replace `# Symlinks auto-update` comment to be more descriptive:
    ```bash
    # Symlinks auto-update — run deploy to apply any new settings
    dotconfigs deploy
    ```

    **3. Scan the entire file for any other stale references:**
    Search for: `dots `, `dotclaude`, old command names. The guide is about Claude Code configuration types (agents, skills, hooks, settings) — it is not about the dotconfigs CLI itself, so most of the content should not reference dotconfigs CLI commands. Only the syncing section and any cross-references need fixing.

    Do NOT rewrite the entire guide. The content about Claude Code configuration types (agents, skills, hooks, settings, CLAUDE.md) is reference documentation about Claude Code itself — it is NOT about the dotconfigs CLI and does not need command name updates. Only fix the explicit issues listed above.
  </action>
  <verify>
    Run: `grep 'TODO' docs/usage-guide.md` — should return 0 matches.
    Run: `grep 'dotclaude' docs/usage-guide.md` — should return 0 matches.
    Run: `head -5 docs/usage-guide.md` — should show clean header with no TODO line.
  </verify>
  <done>Usage guide has no TODO marker, no dotclaude references, and correct syncing instructions.</done>
</task>

</tasks>

<verification>
1. `grep 'TODO' docs/usage-guide.md` returns 0 matches
2. `grep 'dotclaude' docs/usage-guide.md` returns 0 matches
3. First line after title is the description (no TODO)
</verification>

<success_criteria>
- TODO marker removed from usage guide
- All dotclaude references updated to dotconfigs
- Syncing section uses correct repo name and includes deploy step
</success_criteria>

<output>
After completion, create `.planning/phases/09-config-ux-redesign/09-09-SUMMARY.md`
</output>
